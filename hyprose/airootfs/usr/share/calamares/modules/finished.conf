---
# Finished page configuration
restartNowEnabled: true
restartNowChecked: true
restartNowCommand: "systemctl -i reboot"

restartNowLabel: "Restart now"

# Completion message (supports HTML)
finishedText: |
    <h1>🌹 Hyprose is Installed!</h1>
    <p>Your system is ready. Remove the USB drive and restart.</p>
    <p>First boot will:</p>
    <ul>
        <li>Generate colors from your wallpaper</li>
        <li>Initialize the Quickshell widgets</li>
        <li>Show a welcome notification</li>
    </ul>
    <p><b>Keybinds:</b> Press <code>Super+/</code> anytime for help</p>
    <p><b>Have fun ricing!</b></p>
